<!DOCTYPE html>
<html>

<head>
	<meta charset="utf-8">
	<title>WebCampBackendNew</title>
	<link rel="stylesheet" href="app/assets/stylesheets/index.css">
</head>

<body>
	<h1>Books</h1>

	<table>
		<thead>
			<tr>
				<th>Title</th> <th>Body</th> <th colspan="3"></th>
			</tr>
		</thead>

		<tbody>
			<% @books.each do |book| %>
				<tr>
					<td><%= book.title %></td> <td><%= book.body %></td> <td>Show</td> <td>Edit</td> <td>Destroy</td>
				</tr>
			<% end %>
		</tbody>

	</table>


<!-- 新規投稿機能 -->

	<%= form_for(@book, url: '/books') do |f| %>
		<h2>New book</h2>

		<h4>Title</h4>
		<%= f.text_field :title %>

		<h4>Body</h4>
		<%= f.text_area :body %>

		<%= f.submit 'Create Book' %>

	<% end %>


</body>

</html>